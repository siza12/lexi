[package]
name = "lexi-examples"
version = "0.1.0"
edition = "2021"

[dependencies]
lexi-core = { path = "../lexi-core" }
lexi-starknet = { path = "../lexi-starknet" }
tokio = { version = "1.0", features = ["full"] }
clap = { version = "4.0", features = ["derive", "env"] }
dotenv = "0.15"
toml = "0.8"
rig-core.workspace = true
rig-sqlite.workspace = true
sqlite-vec = "0.1"
tokio-rusqlite.workspace = true
chrono = "0.4"

[[example]]
name = "main"
path = "src/main.rs"
